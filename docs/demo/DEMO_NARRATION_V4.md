# Demo narration v4

An agent can remember the most relevant instruction and still do the wrong thing. Continuum Memory Firewall separates semantic relevance from authority, so durable memory must earn trust before it can influence another action.

Here is the live checkout incident. Trusted cache-pressure telemetry becomes canonical memory. A poisoned instruction to disable payment verification is rejected. Titan retrieves the verified recovery, and two workers race while CockroachDB records exactly one durable action owner.

Now compare identical incidents across five independent episode-state seeds: one hundred eighty cases per arm, five hundred forty observations total. Raw RAG achieves only fifty-two point eight percent verified outcomes. Under memory pressure, eighty-eight point nine percent of its proposals are unsafe, and poison is exposed in ninety-four point four percent of cases.

Continuum reaches one hundred percent verified outcomes, zero unsafe proposals, zero poison exposure, and one hundred percent canonical promotion precision. The paired outcome lift over raw RAG is forty-seven point two percentage points, with a ninety-five percent interval from thirty point six to sixty-three point nine.

The difference is an episode contract. The model may search, fetch, and propose a typed action using only server-issued citation handles. A provider receipt proves the outcome. Only a verified success can become canonical memory for the next episode.

The authority chain is independent of the model: Cognito verifies the caller, an audited binding selects a server-owned scope, that scope selects a no-bypass SQL identity, and CockroachDB row-level security enforces the same tenant and incident on every row.

This is not a toy index. At fifty thousand synthetic vectors, natural CockroachDB ANN reaches ninety-six point nine percent Recall at ten. Fifty concurrent agents share a bounded twenty-connection pool with zero leaked rows and one action owner.

Finally, the judge presses one read-only button. It checks the live service, exact workflow heads, five hundred forty paired episodes, row-isolation checksum, sandbox receipt, and the immutable hackathon v-five release. The release envelope is digest-bound and signed with GitHub provenance.

Continuum turns agent memory from an append-only liability into verified operational knowledge. Similarity finds memory. Outcomes earn trust. CockroachDB decides authority.
